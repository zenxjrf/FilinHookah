# ✅ Чеклист деплоя Filin Bot v3.0

## 📋 Перед деплоем

### 1. Проверка файлов
- [ ] Все файлы на месте
- [ ] `.env` создан и заполнен
- [ ] `requirements.txt` актуален

### 2. Проверка переменных окружения
- [ ] `BOT_TOKEN` - получен в @BotFather
- [ ] `DATABASE_URL` - PostgreSQL URL (для production)
- [ ] `WEBAPP_URL` - HTTPS URL твоего приложения
- [ ] `ADMIN_IDS` - Telegram ID администраторов
- [ ] `DB_POOL_SIZE` - 5 для free, 10+ для paid

### 3. Локальное тестирование
```bash
# Проверка зависимостей
pip install -r requirements.txt

# Запуск WebApp
python -m app.run_webapp

# Запуск бота (в другом терминале)
python main.py
```

### 4. Проверка функциональности
- [ ] Бот отвечает на `/start`
- [ ] WebApp открывается
- [ ] Бронирование работает
- [ ] Админ-панель доступна: `/admin`
- [ ] Команда `/broadcast` работает

---

## 🚀 Деплой на Render

### 1. Подготовка
- [ ] Код загружен на GitHub
- [ ] Создан PostgreSQL на Render
- [ ] Скопирован Internal Database URL

### 2. Настройка на Render
- [ ] Создан Web Service
- [ ] Выбран репозиторий
- [ ] Build Command: `pip install -r requirements.txt`
- [ ] Start Command: `uvicorn app.webapp.app:app --host 0.0.0.0 --port $PORT`

### 3. Переменные окружения
```env
BOT_TOKEN=...
DATABASE_URL=postgresql+asyncpg://...
WEBAPP_URL=https://your-app.onrender.com/webapp
ADMIN_IDS=...
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=2
```

### 4. Деплой
- [ ] Нажат "Create Web Service"
- [ ] Деплой успешен (зелёная галочка)
- [ ] Логи чистые (нет ошибок)

---

## 🧪 Пост-деплой проверка

### 1. Проверка доступности
- [ ] `https://your-app.onrender.com/health` → `{"status": "ok"}`
- [ ] `https://your-app.onrender.com/webapp` → HTML страница
- [ ] `https://your-app.onrender.com/static/styles.css` → CSS

### 2. Проверка бота
- [ ] `/start` - бот отвечает, создаёт клиента, подписывает
- [ ] Меню открывается
- [ ] Бронирование работает

### 3. Проверка админ-панели
- [ ] `/admin` - открывается
- [ ] Дашборд показывает статистику
- [ ] Карта столов отображается
- [ ] WebSocket подключён (консоль браузера)

### 4. Проверка WebSocket
- [ ] Открыть `/admin` в двух вкладках
- [ ] Изменить статус брони в одной вкладке
- [ ] Проверить обновление в другой вкладке
- [ ] Появилось уведомление

### 5. Проверка рассылки
- [ ] `/subscribers` - показывает количество
- [ ] `/broadcast` - запускает режим рассылки
- [ ] Отправить сообщение
- [ ] Рассылка доставлена подписчикам

---

## 🔧 Скрипты для проверки

### 1. Проверка готовности
```bash
python scripts/check_deploy.py
```

### 2. Тест WebSocket (локально)
```bash
python scripts/test_websocket.py ws://localhost:10000/ws/admin
```

### 3. Тест WebSocket (production)
```bash
python scripts/test_websocket.py wss://your-app.onrender.com/ws/admin
```

### 4. Тест рассылки
```bash
python scripts/test_broadcast.py
```

---

## 📊 Мониторинг

### Render Dashboard
- [ ] Logs - нет ошибок
- [ ] Metrics - CPU < 50%, Memory < 80%
- [ ] Deployments - последний деплой успешен

### PostgreSQL
- [ ] Connections < pool_size
- [ ] Нет долгих запросов
- [ ] Место на диске доступно

### Бот
- [ ] Отвечает на сообщения
- [ ] Брони создаются
- [ ] Напоминания отправляются

---

## ⚠️ Troubleshooting

### Бот не отвечает
1. Проверь логи: `Render Dashboard → Logs`
2. Проверь `BOT_TOKEN` в переменных
3. Перезапусти сервис: `Manual Deploy → Deploy Latest Commit`

### WebApp не открывается
1. Проверь `WEBAPP_URL` (должен заканчиваться на `/webapp`)
2. Проверь Start Command
3. Проверь логи на ошибки импорта

### WebSocket не подключается
1. Используй `wss://` для HTTPS
2. Проверь CORS настройки
3. Проверь консоль браузера (F12) на ошибки

### Рассылка не работает
1. Проверь `/subscribers` - есть ли подписчики
2. Проверь `BOT_TOKEN`
3. Проверь логи на ошибки отправки

### Ошибки БД
1. Проверь `DATABASE_URL` формат
2. Проверь что PostgreSQL запущен
3. Проверь логи на ошибки подключения

---

## 🎉 Финальный чеклист

- [ ] Все проверки пройдены
- [ ] Бот работает
- [ ] WebApp открывается
- [ ] Админ-панель синхронизируется
- [ ] Рассылки отправляются
- [ ] Логи чистые
- [ ] Мониторинг настроен

**Готово! Твой бот работает в production! 🦉**

---

## 📝 Контакты для поддержки

- GitHub Issues: [ссылка]
- Telegram: [твой контакт]
- Документация: [ссылка на docs]
