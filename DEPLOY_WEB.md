# 🚀 Быстрый деплой через веб-консоль Yandex Cloud

## ⏱️ Время: ~15 минут

---

## Шаг 1: Регистрация (2 мин)

1. Перейди на [console.cloud.yandex.ru](https://console.cloud.yandex.ru)
2. Войди через Яндекс аккаунт
3. Привяжи карту (дадут **5000 ₽** на 60 дней)

---

## Шаг 2: Создание Container Registry (3 мин)

1. В консоли выбери свой **каталог** (по умолчанию `default`)
2. Слева в меню выбери **Container Registry**
3. Нажми **Создать реестр**
4. Название: `filin-registry`
5. Нажми **Создать**
6. **Скопируй ID реестра** (например, `crp8s6dqh7kqj7tqf7kqg7`)

---

## Шаг 3: Подготовка Docker образа (5 мин)

### На компьютере:

```bash
# Перейди в папку проекта
cd "C:\Users\PC 2\PycharmProjects\Filin"

# Авторизуйся в Yandex Container Registry
yc container registry configure --docker-for-registry-id <ТВОЙ_ID_РЕЕСТРА>

# Собери образ
docker build -t cr.yandex/<ТВОЙ_ID_РЕЕСТРА>/filin-bot:latest .

# Загрузи в реестр
docker push cr.yandex/<ТВОЙ_ID_РЕЕСТРА>/filin-bot:latest
```

**Готово!** Образ загружен.

---

## Шаг 4: Создание App Container (3 мин)

1. В консоли слева выбери **Serverless Containers**
2. Нажми **Создать контейнер**
3. Заполни:
   - **Имя**: `filin-bot`
   - **Описание**: Telegram бот Filin Lounge
4. Нажми **Создать**

---

## Шаг 5: Создание ревизии (3 мин)

1. Открой созданный контейнер
2. Нажми **Создать ревизию**
3. Заполни:

### Образ:
- **Из Container Registry**: выбери свой реестр
- **Образ**: `cr.yandex/<ID>/filin-bot:latest`

### Ресурсы:
- **Memory**: 256 MB
- **Cores**: 1
- **Core fraction**: 5%

### Переменные окружения:
```
BOT_TOKEN=8306362120:AAHXCXOXFk_Eam6gbfnwK0f0vTyI16RNFZo
WEBAPP_URL=https://<твой_URL_после_деплоя>
ADMIN_IDS=1698158035,987654321
WORKERS_CHAT_ID=-1003748695791
DATABASE_URL=sqlite+aiosqlite:///./filin.db
LOG_PATH=logs.txt
DEFAULT_SCHEDULE=Ежедневно с 14:00 до 2:00
DEFAULT_CONTACTS=Phone: +7 (000) 000-00-00\nAddress: Example street, 1
```

### Сервисный аккаунт:
- Выбери существующий или создай новый с ролью `serverless.containers.runtime`

4. Нажми **Создать ревизию**

---

## Шаг 6: Получение URL (1 мин)

1. После создания ревизии откроется страница контейнера
2. Скопируй **URL** (например: `https://b8s6dqh7kqj7tqf7kqg7.mksrv.net`)
3. **Обнови переменную WEBAPP_URL** с этим URL

---

## Шаг 7: Обновление бота в Telegram (2 мин)

1. Открой @BotFather в Telegram
2. Отправь `/mybots`
3. Выбери `FilinHookah_bot`
4. Выбери **Menu Button**
5. Отправь новый URL: `https://<твой_URL>.mksrv.net`
6. Отправь название кнопки: `Открыть меню`

---

## ✅ Готово!

Твой бот работает! 🎉

### Проверка:

1. Открой бота @FilinHookah_bot
2. Нажми `/start`
3. Нажми **Открыть меню** — должен открыться Web App

---

## 💰 Стоимость (после гранта 5000 ₽):

| Ресурс | Стоимость/мес |
|--------|---------------|
| App Container (256MB, 1 core) | ~250 ₽ |
| Container Registry | ~50 ₽ |
| **Итого** | **~300-400 ₽** |

---

## 🔧 Управление через консоль:

### Посмотреть логи:
```bash
yc serverless container logs --name filin-bot --tail 100
```

### Создать новую ревизию (после обновлений):
```bash
# Собрать новый образ
docker build -t cr.yandex/<ID>/filin-bot:latest .
docker push cr.yandex/<ID>/filin-bot:latest

# Создать ревизию в консоли или через CLI
yc serverless container revision create \
  --container-name filin-bot \
  --image cr.yandex/<ID>/filin-bot:latest \
  --memory 256m
```

### Остановить контейнер:
```bash
yc serverless container update --name filin-bot --status STOPPED
```

---

## ⚠️ Важные моменты:

1. **SQLite** — файлы сохраняются между перезапусками
2. **HTTPS** — работает автоматически
3. **Таймаут** — контейнер работает постоянно
4. **Логи** — смотри в консоли Yandex Cloud или через `yc serverless container logs`

---

## 🆘 Проблемы и решения:

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверь логи: `yc serverless container logs --name filin-bot` |
| Web App не открывается | Проверь WEBAPP_URL в переменных окружения |
| Ошибка "Container not found" | Создай контейнер заново в консоли |
| Закончились деньги на гранте | Пополни баланс (~500 ₽ хватит на месяц) |

---

**Удачи! 🚀**

Если что-то не работает — пиши в поддержку Yandex Cloud или смотри документацию: [cloud.yandex.ru/docs/serverless-containers/](https://cloud.yandex.ru/docs/serverless-containers/)
