# ===========================================
# Filin Bot v3.0 - Environment Variables
# ===========================================
# Для Render.com
# ===========================================

# -------------------------------------------
# TELEGRAM BOT
# -------------------------------------------
# Получите токен в @BotFather
BOT_TOKEN=123456:replace-me

# -------------------------------------------
# DATABASE (PostgreSQL для production)
# -------------------------------------------
# Render PostgreSQL (Internal Database URL):
# Формат: postgresql+asyncpg://user:password@host:port/dbname
# Пример: postgresql+asyncpg://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech:5432/filin
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/filin

# -------------------------------------------
# WEB APP URL
# -------------------------------------------
# Для Render: https://your-app.onrender.com/webapp
# Для Railway: https://your-app.up.railway.app/webapp
# Для локальной разработки: http://localhost:8000/webapp
WEBAPP_URL=https://your-app.onrender.com/webapp

# -------------------------------------------
# ADMIN IDS
# -------------------------------------------
# Telegram ID администраторов (через запятую)
# Узнать свой ID: @userinfobot
ADMIN_IDS=123456789,987654321

# -------------------------------------------
# WORKERS CHAT ID (опционально)
# -------------------------------------------
# ID чата работников для уведомлений о бронях
# Если пусто - уведомления отправляются админам в ЛС
WORKERS_CHAT_ID=

# -------------------------------------------
# LOGGING
# -------------------------------------------
LOG_PATH=logs.txt

# -------------------------------------------
# DEFAULT SETTINGS
# -------------------------------------------
DEFAULT_SCHEDULE=Ежедневно с 14:00 до 2:00
DEFAULT_CONTACTS=Phone: +7 (000) 000-00-00\nAddress: Example street, 1

# -------------------------------------------
# POSTGRESQL POOL SETTINGS (для Render)
# -------------------------------------------
# Для бесплатного тарифа Render: меньше значений
# Для платного: можно увеличить

# Размер пула соединений (5 для free, 10+ для paid)
DB_POOL_SIZE=5

# Максимальное количество соединений сверх pool_size (2 для free, 5+ для paid)
DB_MAX_OVERFLOW=2

# Таймаут ожидания соединения из пула (секунды)
DB_POOL_TIMEOUT=30

# Время пересоздания соединения (секунды, 30 мин = 1800)
DB_POOL_RECYCLE=1800

# -------------------------------------------
# RENDER SPECIFIC (автоматически задаются)
# -------------------------------------------
# PORT - автоматически задаётся Render (не нужно в .env)
# RENDER_EXTERNAL_URL - автоматически задаётся Render
# Эти переменные не нужны в .env, Render их задаёт сам
